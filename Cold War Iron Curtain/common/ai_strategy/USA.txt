help_Greece = {
    enable = {
        AND = {
            GRE = { has_war_with = PDG }
			tag = USA
        }
    }
 
    abort = {
        NOT = { country_exists = USA }
		GRE = { NOT = { has_war_with = PDG } }
    }
   
    ai_strategy = {
        type = befriend
        id = "GRE"            
        value = 200
    }
}
