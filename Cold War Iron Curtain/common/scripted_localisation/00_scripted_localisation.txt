# Allows you to create your own dynamic keys
# to be called in localization.
# defined_text -> this is it, we're defining the text
# text -> a discrete entry that can be picked to display in loc.
# trigger -> determines if a text entry will be picked or not.
# (The triggers need to be valid for the scope the key is called in
# (eg Root or From.From).)
# localization_key -> points to the localization key
# that'll be used if trigger passes

defined_text = { 
	name = USASpaceResearchWindowName
	text = {
		trigger = {
			USA = { has_country_flag = USA_NASA }
		}
		localization_key = USA_SpaceWindow_new
	}
	text = { localization_key = USA_SpaceWindow_old }
}

defined_text = { 
	name = USASpaceResearchWindowNameShort
	text = {
		trigger = {
			USA = { has_country_flag = USA_NASA }
		}
		localization_key = USA_SpaceWindowShort_new
	}
	text = { localization_key = USA_SpaceWindowShort_old }
}

defined_text = { 
	name = USASpaceResearchWindowNameTooltip
	text = {
		trigger = {
			USA = { has_country_flag = USA_NASA }
		}
		localization_key = USA_SpaceWindowShortTooltip_new
	}
	text = { localization_key = USA_SpaceWindowShortTooltip_old }
}

defined_text = { 
	name = SpaceEventText
	text = {
		trigger = {
			USA = { has_country_flag = atlas_sm65a_rocket_tank }
		}
		localization_key = atlas_sm65a_rocket_tank_event_text
	}
	text = { localization_key = placeholder }
}

defined_text = { 
	name = SpaceEventHeadline
	text = {
		trigger = {
			USA = { has_country_flag = atlas_sm65a_rocket_tank }
		}
		localization_key = atlas_sm65a_rocket_tank_headline_text
	}
	text = { localization_key = placeholder }
}

defined_text = { 
	name = SpaceEventYesTooltip
	text = {
		trigger = {
			USA = { has_country_flag = atlas_sm65a_rocket_tank }
		}
		localization_key = atlas_sm65a_rocket_tank_yes_text
	}
	text = {
		trigger = {
			USA = { has_country_flag = atlas_sm65a_rocket_tank_done }
		}
		localization_key = atlas_sm65a_rocket_tank_done_text
	}
	text = { localization_key = placeholder }
}
defined_text = { 
	name = religious_plurality
	text = {
		trigger = {
			check_variable = { atheism_pop > bahai_pop }
			check_variable = { atheism_pop > buddhism_pop }
			check_variable = { atheism_pop > catholic_pop }
			check_variable = { atheism_pop > caodaism_pop }
			check_variable = { atheism_pop > chinese_traditional_religion_pop }
			check_variable = { atheism_pop > coptic_pop }
			check_variable = { atheism_pop > druze_pop }
			check_variable = { atheism_pop > hinduism_pop }
			check_variable = { atheism_pop > ibadi_pop }
			check_variable = { atheism_pop > jainism_pop }
			check_variable = { atheism_pop > juche_pop }
			check_variable = { atheism_pop > judaism_pop }
			check_variable = { atheism_pop > native_religion_pop }
			check_variable = { atheism_pop > nestorian_pop }
			check_variable = { atheism_pop > orthodox_pop }
			check_variable = { atheism_pop > protestant_pop }
			check_variable = { atheism_pop > mormon_pop }
			check_variable = { atheism_pop > shia_pop }
			check_variable = { atheism_pop > shinto_pop }
			check_variable = { atheism_pop > sikhism_pop }
			check_variable = { atheism_pop > sunni_pop }
			check_variable = { atheism_pop > tenrikyo_pop }
			check_variable = { atheism_pop > yazidi_pop }
			check_variable = { atheism_pop > zoroastrian_pop }
		}
		localization_key = atheist_majority_text
	}
	text = {
		trigger = {
			check_variable = { bahai_pop > atheism_pop }
			#check_variable = { bahai_pop > bahai_pop }
			check_variable = { bahai_pop > buddhism_pop }
			check_variable = { bahai_pop > catholic_pop }
			check_variable = { bahai_pop > caodaism_pop }
			check_variable = { bahai_pop > chinese_traditional_religion_pop }
			check_variable = { bahai_pop > coptic_pop }
			check_variable = { bahai_pop > druze_pop }
			check_variable = { bahai_pop > hinduism_pop }
			check_variable = { bahai_pop > ibadi_pop }
			check_variable = { bahai_pop > jainism_pop }
			check_variable = { bahai_pop > juche_pop }
			check_variable = { bahai_pop > judaism_pop }
			check_variable = { bahai_pop > native_religion_pop }
			check_variable = { bahai_pop > nestorian_pop }
			check_variable = { bahai_pop > orthodox_pop }
			check_variable = { bahai_pop > protestant_pop }
			check_variable = { bahai_pop > mormon_pop }
			check_variable = { bahai_pop > shia_pop }
			check_variable = { bahai_pop > shinto_pop }
			check_variable = { bahai_pop > sikhism_pop }
			check_variable = { bahai_pop > sunni_pop }
			check_variable = { bahai_pop > tenrikyo_pop }
			check_variable = { bahai_pop > yazidi_pop }
			check_variable = { bahai_pop > zoroastrian_pop }
		}
		localization_key = bahai_majority_text
	}
	text = {
		trigger = {
			check_variable = { buddhism_pop > atheism_pop }
			check_variable = { buddhism_pop > bahai_pop }
			#check_variable = { buddhism_pop > buddhism_pop }
			check_variable = { buddhism_pop > catholic_pop }
			check_variable = { buddhism_pop > caodaism_pop }
			check_variable = { buddhism_pop > chinese_traditional_religion_pop }
			check_variable = { buddhism_pop > coptic_pop }
			check_variable = { buddhism_pop > druze_pop }
			check_variable = { buddhism_pop > hinduism_pop }
			check_variable = { buddhism_pop > ibadi_pop }
			check_variable = { buddhism_pop > jainism_pop }
			check_variable = { buddhism_pop > juche_pop }
			check_variable = { buddhism_pop > judaism_pop }
			check_variable = { buddhism_pop > native_religion_pop }
			check_variable = { buddhism_pop > nestorian_pop }
			check_variable = { buddhism_pop > orthodox_pop }
			check_variable = { buddhism_pop > protestant_pop }
			check_variable = { buddhism_pop > mormon_pop }
			check_variable = { buddhism_pop > shia_pop }
			check_variable = { buddhism_pop > shinto_pop }
			check_variable = { buddhism_pop > sikhism_pop }
			check_variable = { buddhism_pop > sunni_pop }
			check_variable = { buddhism_pop > tenrikyo_pop }
			check_variable = { buddhism_pop > yazidi_pop }
			check_variable = { buddhism_pop > zoroastrian_pop }
		}
		localization_key = buddhism_majority_text
	}
	text = {
		trigger = {
			check_variable = { catholic_pop > atheism_pop }
			check_variable = { catholic_pop > bahai_pop }
			check_variable = { catholic_pop > buddhism }
			#check_variable = { catholic_pop > catholic_pop }
			check_variable = { catholic_pop > caodaism_pop }
			check_variable = { catholic_pop > chinese_traditional_religion_pop }
			check_variable = { catholic_pop > coptic_pop }
			check_variable = { catholic_pop > druze_pop }
			check_variable = { catholic_pop > hinduism_pop }
			check_variable = { catholic_pop > ibadi_pop }
			check_variable = { catholic_pop > jainism_pop }
			check_variable = { catholic_pop > juche_pop }
			check_variable = { catholic_pop > judaism_pop }
			check_variable = { catholic_pop > native_religion_pop }
			check_variable = { catholic_pop > nestorian_pop }
			check_variable = { catholic_pop > orthodox_pop }
			check_variable = { catholic_pop > protestant_pop }
			check_variable = { catholic_pop > mormon_pop }
			check_variable = { catholic_pop > shia_pop }
			check_variable = { catholic_pop > shinto_pop }
			check_variable = { catholic_pop > sikhism_pop }
			check_variable = { catholic_pop > sunni_pop }
			check_variable = { catholic_pop > tenrikyo_pop }
			check_variable = { catholic_pop > yazidi_pop }
			check_variable = { catholic_pop > zoroastrian_pop }
		}
		localization_key = catholic_majority_text
	}
	text = {
		trigger = {
			check_variable = { caodaism_pop > atheism_pop }
			check_variable = { caodaism_pop > bahai_pop }
			check_variable = { caodaism_pop > buddhism }
			check_variable = { caodaism_pop > catholic_pop }
			#check_variable = { caodaism_pop > caodaism_pop }
			check_variable = { caodaism_pop > chinese_traditional_religion_pop }
			check_variable = { caodaism_pop > coptic_pop }
			check_variable = { caodaism_pop > druze_pop }
			check_variable = { caodaism_pop > hinduism_pop }
			check_variable = { caodaism_pop > ibadi_pop }
			check_variable = { caodaism_pop > jainism_pop }
			check_variable = { caodaism_pop > juche_pop }
			check_variable = { caodaism_pop > judaism_pop }
			check_variable = { caodaism_pop > native_religion_pop }
			check_variable = { caodaism_pop > nestorian_pop }
			check_variable = { caodaism_pop > orthodox_pop }
			check_variable = { caodaism_pop > protestant_pop }
			check_variable = { caodaism_pop > mormon_pop }
			check_variable = { caodaism_pop > shia_pop }
			check_variable = { caodaism_pop > shinto_pop }
			check_variable = { caodaism_pop > sikhism_pop }
			check_variable = { caodaism_pop > sunni_pop }
			check_variable = { caodaism_pop > tenrikyo_pop }
			check_variable = { caodaism_pop > yazidi_pop }
			check_variable = { caodaism_pop > zoroastrian_pop }
		}
		localization_key = caodaism_majority_text
	}
	text = {
		trigger = {
			check_variable = { chinese_traditional_religion_pop > atheism_pop }
			check_variable = { chinese_traditional_religion_pop > bahai_pop }
			check_variable = { chinese_traditional_religion_pop > buddhism }
			check_variable = { chinese_traditional_religion_pop > catholic_pop }
			check_variable = { chinese_traditional_religion_pop > caodaism_pop }
			#check_variable = { chinese_traditional_religion_pop > chinese_traditional_religion_pop }
			check_variable = { chinese_traditional_religion_pop > coptic_pop }
			check_variable = { chinese_traditional_religion_pop > druze_pop }
			check_variable = { chinese_traditional_religion_pop > hinduism_pop }
			check_variable = { chinese_traditional_religion_pop > ibadi_pop }
			check_variable = { chinese_traditional_religion_pop > jainism_pop }
			check_variable = { chinese_traditional_religion_pop > juche_pop }
			check_variable = { chinese_traditional_religion_pop > judaism_pop }
			check_variable = { chinese_traditional_religion_pop > native_religion_pop }
			check_variable = { chinese_traditional_religion_pop > nestorian_pop }
			check_variable = { chinese_traditional_religion_pop > orthodox_pop }
			check_variable = { chinese_traditional_religion_pop > protestant_pop }
			check_variable = { chinese_traditional_religion_pop > mormon_pop }
			check_variable = { chinese_traditional_religion_pop > shia_pop }
			check_variable = { chinese_traditional_religion_pop > shinto_pop }
			check_variable = { chinese_traditional_religion_pop > sikhism_pop }
			check_variable = { chinese_traditional_religion_pop > sunni_pop }
			check_variable = { chinese_traditional_religion_pop > tenrikyo_pop }
			check_variable = { chinese_traditional_religion_pop > yazidi_pop }
			check_variable = { chinese_traditional_religion_pop > zoroastrian_pop }
		}
		localization_key = chinese_traditional_religion_majority_text
	}
	text = {
		trigger = {
			check_variable = { coptic_pop > atheism_pop }
			check_variable = { coptic_pop > bahai_pop }
			check_variable = { coptic_pop > buddhism }
			check_variable = { coptic_pop > catholic_pop }
			check_variable = { coptic_pop > caodaism_pop }
			check_variable = { coptic_pop > chinese_traditional_religion_pop }
			#check_variable = { coptic_pop > coptic_pop }
			check_variable = { coptic_pop > druze_pop }
			check_variable = { coptic_pop > hinduism_pop }
			check_variable = { coptic_pop > ibadi_pop }
			check_variable = { coptic_pop > jainism_pop }
			check_variable = { coptic_pop > juche_pop }
			check_variable = { coptic_pop > judaism_pop }
			check_variable = { coptic_pop > native_religion_pop }
			check_variable = { coptic_pop > nestorian_pop }
			check_variable = { coptic_pop > orthodox_pop }
			check_variable = { coptic_pop > protestant_pop }
			check_variable = { coptic_pop > mormon_pop }
			check_variable = { coptic_pop > shia_pop }
			check_variable = { coptic_pop > shinto_pop }
			check_variable = { coptic_pop > sikhism_pop }
			check_variable = { coptic_pop > sunni_pop }
			check_variable = { coptic_pop > tenrikyo_pop }
			check_variable = { coptic_pop > yazidi_pop }
			check_variable = { coptic_pop > zoroastrian_pop }
		}
		localization_key = coptic_majority_text
	}
	text = {
		trigger = {
			check_variable = { druze_pop > atheism_pop }
			check_variable = { druze_pop > bahai_pop }
			check_variable = { druze_pop > buddhism }
			check_variable = { druze_pop > catholic_pop }
			check_variable = { druze_pop > caodaism_pop }
			check_variable = { druze_pop > chinese_traditional_religion_pop }
			check_variable = { druze_pop > coptic_pop }
			#check_variable = { druze_pop > druze_pop }
			check_variable = { druze_pop > hinduism_pop }
			check_variable = { druze_pop > ibadi_pop }
			check_variable = { druze_pop > jainism_pop }
			check_variable = { druze_pop > juche_pop }
			check_variable = { druze_pop > judaism_pop }
			check_variable = { druze_pop > native_religion_pop }
			check_variable = { druze_pop > nestorian_pop }
			check_variable = { druze_pop > orthodox_pop }
			check_variable = { druze_pop > protestant_pop }
			check_variable = { druze_pop > mormon_pop }
			check_variable = { druze_pop > shia_pop }
			check_variable = { druze_pop > shinto_pop }
			check_variable = { druze_pop > sikhism_pop }
			check_variable = { druze_pop > sunni_pop }
			check_variable = { druze_pop > tenrikyo_pop }
			check_variable = { druze_pop > yazidi_pop }
			check_variable = { druze_pop > zoroastrian_pop }
		}
		localization_key = druze_majority_text
	}
	text = {
		trigger = {
			check_variable = { hinduism_pop > atheism_pop }
			check_variable = { hinduism_pop > bahai_pop }
			check_variable = { hinduism_pop > buddhism }
			check_variable = { hinduism_pop > catholic_pop }
			check_variable = { hinduism_pop > caodaism_pop }
			check_variable = { hinduism_pop > chinese_traditional_religion_pop }
			check_variable = { hinduism_pop > coptic_pop }
			check_variable = { hinduism_pop > druze_pop }
			#check_variable = { hinduism_pop > hinduism_pop }
			check_variable = { hinduism_pop > ibadi_pop }
			check_variable = { hinduism_pop > jainism_pop }
			check_variable = { hinduism_pop > juche_pop }
			check_variable = { hinduism_pop > judaism_pop }
			check_variable = { hinduism_pop > native_religion_pop }
			check_variable = { hinduism_pop > nestorian_pop }
			check_variable = { hinduism_pop > orthodox_pop }
			check_variable = { hinduism_pop > protestant_pop }
			check_variable = { hinduism_pop > mormon_pop }
			check_variable = { hinduism_pop > shia_pop }
			check_variable = { hinduism_pop > shinto_pop }
			check_variable = { hinduism_pop > sikhism_pop }
			check_variable = { hinduism_pop > sunni_pop }
			check_variable = { hinduism_pop > tenrikyo_pop }
			check_variable = { hinduism_pop > yazidi_pop }
			check_variable = { hinduism_pop > zoroastrian_pop }
		}
		localization_key = hindu_majority_text
	}
	text = {
		trigger = {
			check_variable = { ibadi_pop > atheism_pop }
			check_variable = { ibadi_pop > bahai_pop }
			check_variable = { ibadi_pop > buddhism }
			check_variable = { ibadi_pop > catholic_pop }
			check_variable = { ibadi_pop > caodaism_pop }
			check_variable = { ibadi_pop > chinese_traditional_religion_pop }
			check_variable = { ibadi_pop > coptic_pop }
			check_variable = { ibadi_pop > druze_pop }
			check_variable = { ibadi_pop > hinduism_pop }
			#check_variable = { ibadi_pop > ibadi_pop }
			check_variable = { ibadi_pop > jainism_pop }
			check_variable = { ibadi_pop > juche_pop }
			check_variable = { ibadi_pop > judaism_pop }
			check_variable = { ibadi_pop > native_religion_pop }
			check_variable = { ibadi_pop > nestorian_pop }
			check_variable = { ibadi_pop > orthodox_pop }
			check_variable = { ibadi_pop > protestant_pop }
			check_variable = { ibadi_pop > mormon_pop }
			check_variable = { ibadi_pop > shia_pop }
			check_variable = { ibadi_pop > shinto_pop }
			check_variable = { ibadi_pop > sikhism_pop }
			check_variable = { ibadi_pop > sunni_pop }
			check_variable = { ibadi_pop > tenrikyo_pop }
			check_variable = { ibadi_pop > yazidi_pop }
			check_variable = { ibadi_pop > zoroastrian_pop }
		}
		localization_key = ibadi_majority_text
	}
	text = {
		trigger = {
			check_variable = { jainism_pop > atheism_pop }
			check_variable = { jainism_pop > bahai_pop }
			check_variable = { jainism_pop > buddhism }
			check_variable = { jainism_pop > catholic_pop }
			check_variable = { jainism_pop > caodaism_pop }
			check_variable = { jainism_pop > chinese_traditional_religion_pop }
			check_variable = { jainism_pop > coptic_pop }
			check_variable = { jainism_pop > druze_pop }
			check_variable = { jainism_pop > hinduism_pop }
			check_variable = { jainism_pop > ibadi_pop }
			#check_variable = { jainism_pop > jainism_pop }
			check_variable = { jainism_pop > juche_pop }
			check_variable = { jainism_pop > judaism_pop }
			check_variable = { jainism_pop > native_religion_pop }
			check_variable = { jainism_pop > nestorian_pop }
			check_variable = { jainism_pop > orthodox_pop }
			check_variable = { jainism_pop > protestant_pop }
			check_variable = { jainism_pop > mormon_pop }
			check_variable = { jainism_pop > shia_pop }
			check_variable = { jainism_pop > shinto_pop }
			check_variable = { jainism_pop > sikhism_pop }
			check_variable = { jainism_pop > sunni_pop }
			check_variable = { jainism_pop > tenrikyo_pop }
			check_variable = { jainism_pop > yazidi_pop }
			check_variable = { jainism_pop > zoroastrian_pop }
		}
		localization_key = jainism_majority_text
		
	}
	text = {
		trigger = {
			check_variable = { juche_pop > atheism_pop }
			check_variable = { juche_pop > bahai_pop }
			check_variable = { juche_pop > buddhism }
			check_variable = { juche_pop > catholic_pop }
			check_variable = { juche_pop > caodaism_pop }
			check_variable = { juche_pop > chinese_traditional_religion_pop }
			check_variable = { juche_pop > coptic_pop }
			check_variable = { juche_pop > druze_pop }
			check_variable = { juche_pop > hinduism_pop }
			check_variable = { juche_pop > ibadi_pop }
			check_variable = { juche_pop > jainism_pop }
			#check_variable = { juche_pop > juche_pop }
			check_variable = { juche_pop > judaism_pop }
			check_variable = { juche_pop > native_religion_pop }
			check_variable = { juche_pop > nestorian_pop }
			check_variable = { juche_pop > orthodox_pop }
			check_variable = { juche_pop > protestant_pop }
			check_variable = { juche_pop > mormon_pop }
			check_variable = { juche_pop > shia_pop }
			check_variable = { juche_pop > shinto_pop }
			check_variable = { juche_pop > sikhism_pop }
			check_variable = { juche_pop > sunni_pop }
			check_variable = { juche_pop > tenrikyo_pop }
			check_variable = { juche_pop > yazidi_pop }
			check_variable = { juche_pop > zoroastrian_pop }
		}
		localization_key = juche_majority_text
	}
	text = {
		trigger = {
			check_variable = { judaism_pop > atheism_pop }
			check_variable = { judaism_pop > bahai_pop }
			check_variable = { judaism_pop > buddhism }
			check_variable = { judaism_pop > catholic_pop }
			check_variable = { judaism_pop > caodaism_pop }
			check_variable = { judaism_pop > chinese_traditional_religion_pop }
			check_variable = { judaism_pop > coptic_pop }
			check_variable = { judaism_pop > druze_pop }
			check_variable = { judaism_pop > hinduism_pop }
			check_variable = { judaism_pop > ibadi_pop }
			check_variable = { judaism_pop > jainism_pop }
			check_variable = { judaism_pop > juche_pop }
			#check_variable = { judaism_pop > judaism_pop }
			check_variable = { judaism_pop > native_religion_pop }
			check_variable = { judaism_pop > nestorian_pop }
			check_variable = { judaism_pop > orthodox_pop }
			check_variable = { judaism_pop > protestant_pop }
			check_variable = { judaism_pop > mormon_pop }
			check_variable = { judaism_pop > shia_pop }
			check_variable = { judaism_pop > shinto_pop }
			check_variable = { judaism_pop > sikhism_pop }
			check_variable = { judaism_pop > sunni_pop }
			check_variable = { judaism_pop > tenrikyo_pop }
			check_variable = { judaism_pop > yazidi_pop }
			check_variable = { judaism_pop > zoroastrian_pop }
		}
		localization_key = judaism_majority_text
	}
	text = {
		trigger = {
			check_variable = { native_religion_pop > atheism_pop }
			check_variable = { native_religion_pop > bahai_pop }
			check_variable = { native_religion_pop > buddhism }
			check_variable = { native_religion_pop > catholic_pop }
			check_variable = { native_religion_pop > caodaism_pop }
			check_variable = { native_religion_pop > chinese_traditional_religion_pop }
			check_variable = { native_religion_pop > coptic_pop }
			check_variable = { native_religion_pop > druze_pop }
			check_variable = { native_religion_pop > hinduism_pop }
			check_variable = { native_religion_pop > ibadi_pop }
			check_variable = { native_religion_pop > jainism_pop }
			check_variable = { native_religion_pop > juche_pop }
			check_variable = { native_religion_pop > judaism_pop }
			#check_variable = { native_religion_pop > native_religion_pop }
			check_variable = { native_religion_pop > nestorian_pop }
			check_variable = { native_religion_pop > orthodox_pop }
			check_variable = { native_religion_pop > protestant_pop }
			check_variable = { native_religion_pop > mormon_pop }
			check_variable = { native_religion_pop > shia_pop }
			check_variable = { native_religion_pop > shinto_pop }
			check_variable = { native_religion_pop > sikhism_pop }
			check_variable = { native_religion_pop > sunni_pop }
			check_variable = { native_religion_pop > tenrikyo_pop }
			check_variable = { native_religion_pop > yazidi_pop }
			check_variable = { native_religion_pop > zoroastrian_pop }
		}
		localization_key = native_religion_majority_text
	}
	text = {
		trigger = {
			check_variable = { nestorian_pop > atheism_pop }
			check_variable = { nestorian_pop > bahai_pop }
			check_variable = { nestorian_pop > buddhism }
			check_variable = { nestorian_pop > catholic_pop }
			check_variable = { nestorian_pop > caodaism_pop }
			check_variable = { nestorian_pop > chinese_traditional_religion_pop }
			check_variable = { nestorian_pop > coptic_pop }
			check_variable = { nestorian_pop > druze_pop }
			check_variable = { nestorian_pop > hinduism_pop }
			check_variable = { nestorian_pop > ibadi_pop }
			check_variable = { nestorian_pop > jainism_pop }
			check_variable = { nestorian_pop > juche_pop }
			check_variable = { nestorian_pop > judaism_pop }
			check_variable = { nestorian_pop > native_religion_pop }
			#check_variable = { nestorian_pop > nestorian_pop }
			check_variable = { nestorian_pop > orthodox_pop }
			check_variable = { nestorian_pop > protestant_pop }
			check_variable = { nestorian_pop > mormon_pop }
			check_variable = { nestorian_pop > shia_pop }
			check_variable = { nestorian_pop > shinto_pop }
			check_variable = { nestorian_pop > sikhism_pop }
			check_variable = { nestorian_pop > sunni_pop }
			check_variable = { nestorian_pop > tenrikyo_pop }
			check_variable = { nestorian_pop > yazidi_pop }
			check_variable = { nestorian_pop > zoroastrian_pop }
		}
		localization_key = nestorian_majority_text
	}
	text = {
		trigger = {
			check_variable = { orthodox_pop > atheism_pop }
			check_variable = { orthodox_pop > bahai_pop }
			check_variable = { orthodox_pop > buddhism }
			check_variable = { orthodox_pop > catholic_pop }
			check_variable = { orthodox_pop > caodaism_pop }
			check_variable = { orthodox_pop > chinese_traditional_religion_pop }
			check_variable = { orthodox_pop > coptic_pop }
			check_variable = { orthodox_pop > druze_pop }
			check_variable = { orthodox_pop > hinduism_pop }
			check_variable = { orthodox_pop > ibadi_pop }
			check_variable = { orthodox_pop > jainism_pop }
			check_variable = { orthodox_pop > juche_pop }
			check_variable = { orthodox_pop > judaism_pop }
			check_variable = { orthodox_pop > native_religion_pop }
			check_variable = { orthodox_pop > nestorian_pop }
			#check_variable = { orthodox_pop > orthodox_pop }
			check_variable = { orthodox_pop > protestant_pop }
			check_variable = { orthodox_pop > mormon_pop }
			check_variable = { orthodox_pop > shia_pop }
			check_variable = { orthodox_pop > shinto_pop }
			check_variable = { orthodox_pop > sikhism_pop }
			check_variable = { orthodox_pop > sunni_pop }
			check_variable = { orthodox_pop > tenrikyo_pop }
			check_variable = { orthodox_pop > yazidi_pop }
			check_variable = { orthodox_pop > zoroastrian_pop }
		}
		localization_key = orthodox_majority_text
	}
	text = {
		trigger = {
			check_variable = { protestant_pop > atheism_pop }
			check_variable = { protestant_pop > bahai_pop }
			check_variable = { protestant_pop > buddhism }
			check_variable = { protestant_pop > catholic_pop }
			check_variable = { protestant_pop > caodaism_pop }
			check_variable = { protestant_pop > chinese_traditional_religion_pop }
			check_variable = { protestant_pop > coptic_pop }
			check_variable = { protestant_pop > druze_pop }
			check_variable = { protestant_pop > hinduism_pop }
			check_variable = { protestant_pop > ibadi_pop }
			check_variable = { protestant_pop > jainism_pop }
			check_variable = { protestant_pop > juche_pop }
			check_variable = { protestant_pop > judaism_pop }
			check_variable = { protestant_pop > native_religion_pop }
			check_variable = { protestant_pop > nestorian_pop }
			check_variable = { protestant_pop > orthodox_pop }
			#check_variable = { protestant_pop > protestant_pop }
			check_variable = { protestant_pop > mormon_pop }
			check_variable = { protestant_pop > shia_pop }
			check_variable = { protestant_pop > shinto_pop }
			check_variable = { protestant_pop > sikhism_pop }
			check_variable = { protestant_pop > sunni_pop }
			check_variable = { protestant_pop > tenrikyo_pop }
			check_variable = { protestant_pop > yazidi_pop }
			check_variable = { protestant_pop > zoroastrian_pop }
		}
		localization_key = protestant_majority_text
	}
	text = {
		trigger = {
			check_variable = { mormon_pop > atheism_pop }
			check_variable = { mormon_pop > bahai_pop }
			check_variable = { mormon_pop > buddhism }
			check_variable = { mormon_pop > catholic_pop }
			check_variable = { mormon_pop > caodaism_pop }
			check_variable = { mormon_pop > chinese_traditional_religion_pop }
			check_variable = { mormon_pop > coptic_pop }
			check_variable = { mormon_pop > druze_pop }
			check_variable = { mormon_pop > hinduism_pop }
			check_variable = { mormon_pop > ibadi_pop }
			check_variable = { mormon_pop > jainism_pop }
			check_variable = { mormon_pop > juche_pop }
			check_variable = { mormon_pop > judaism_pop }
			check_variable = { mormon_pop > native_religion_pop }
			check_variable = { mormon_pop > nestorian_pop }
			check_variable = { mormon_pop > orthodox_pop }
			check_variable = { mormon_pop > protestant_pop }
			#check_variable = { mormon_pop > mormon_pop }
			check_variable = { mormon_pop > shia_pop }
			check_variable = { mormon_pop > shinto_pop }
			check_variable = { mormon_pop > sikhism_pop }
			check_variable = { mormon_pop > sunni_pop }
			check_variable = { mormon_pop > tenrikyo_pop }
			check_variable = { mormon_pop > yazidi_pop }
			check_variable = { mormon_pop > zoroastrian_pop }
		}
		localization_key = mormon_majority_text
	}
	text = {
		trigger = {
			check_variable = { shia_pop > atheism_pop }
			check_variable = { shia_pop > bahai_pop }
			check_variable = { shia_pop > buddhism }
			check_variable = { shia_pop > catholic_pop }
			check_variable = { shia_pop > caodaism_pop }
			check_variable = { shia_pop > chinese_traditional_religion_pop }
			check_variable = { shia_pop > coptic_pop }
			check_variable = { shia_pop > druze_pop }
			check_variable = { shia_pop > hinduism_pop }
			check_variable = { shia_pop > ibadi_pop }
			check_variable = { shia_pop > jainism_pop }
			check_variable = { shia_pop > juche_pop }
			check_variable = { shia_pop > judaism_pop }
			check_variable = { shia_pop > native_religion_pop }
			check_variable = { shia_pop > nestorian_pop }
			check_variable = { shia_pop > orthodox_pop }
			check_variable = { shia_pop > protestant_pop }
			check_variable = { shia_pop > mormon_pop }
			#check_variable = { shia_pop > shia_pop }
			check_variable = { shia_pop > shinto_pop }
			check_variable = { shia_pop > sikhism_pop }
			check_variable = { shia_pop > sunni_pop }
			check_variable = { shia_pop > tenrikyo_pop }
			check_variable = { shia_pop > yazidi_pop }
			check_variable = { shia_pop > zoroastrian_pop }
		}
		localization_key = shia_majority_text
	}
	text = {
		trigger = {
			check_variable = { shinto_pop > atheism_pop }
			check_variable = { shinto_pop > bahai_pop }
			check_variable = { shinto_pop > buddhism }
			check_variable = { shinto_pop > catholic_pop }
			check_variable = { shinto_pop > caodaism_pop }
			check_variable = { shinto_pop > chinese_traditional_religion_pop }
			check_variable = { shinto_pop > coptic_pop }
			check_variable = { shinto_pop > druze_pop }
			check_variable = { shinto_pop > hinduism_pop }
			check_variable = { shinto_pop > ibadi_pop }
			check_variable = { shinto_pop > jainism_pop }
			check_variable = { shinto_pop > juche_pop }
			check_variable = { shinto_pop > judaism_pop }
			check_variable = { shinto_pop > native_religion_pop }
			check_variable = { shinto_pop > nestorian_pop }
			check_variable = { shinto_pop > orthodox_pop }
			check_variable = { shinto_pop > protestant_pop }
			check_variable = { shinto_pop > mormon_pop }
			check_variable = { shinto_pop > shia_pop }
			#check_variable = { shinto_pop > shinto_pop }
			check_variable = { shinto_pop > sikhism_pop }
			check_variable = { shinto_pop > sunni_pop }
			check_variable = { shinto_pop > tenrikyo_pop }
			check_variable = { shinto_pop > yazidi_pop }
			check_variable = { shinto_pop > zoroastrian_pop }
		}
		localization_key = shinto_majority_text
	}
	text = {
		trigger = {
			check_variable = { sikhism_pop > atheism_pop }
			check_variable = { sikhism_pop > bahai_pop }
			check_variable = { sikhism_pop > buddhism }
			check_variable = { sikhism_pop > catholic_pop }
			check_variable = { sikhism_pop > caodaism_pop }
			check_variable = { sikhism_pop > chinese_traditional_religion_pop }
			check_variable = { sikhism_pop > coptic_pop }
			check_variable = { sikhism_pop > druze_pop }
			check_variable = { sikhism_pop > hinduism_pop }
			check_variable = { sikhism_pop > ibadi_pop }
			check_variable = { sikhism_pop > jainism_pop }
			check_variable = { sikhism_pop > juche_pop }
			check_variable = { sikhism_pop > judaism_pop }
			check_variable = { sikhism_pop > native_religion_pop }
			check_variable = { sikhism_pop > nestorian_pop }
			check_variable = { sikhism_pop > orthodox_pop }
			check_variable = { sikhism_pop > protestant_pop }
			check_variable = { sikhism_pop > mormon_pop }
			check_variable = { sikhism_pop > shia_pop }
			check_variable = { sikhism_pop > shinto_pop }
			check_variable = { sikhism_pop > sikhism_pop }
			#check_variable = { sikhism_pop > sunni_pop }
			check_variable = { sikhism_pop > tenrikyo_pop }
			check_variable = { sikhism_pop > yazidi_pop }
			check_variable = { sikhism_pop > zoroastrian_pop }
		}
		localization_key = sikhism_majority_text
	}
	text = {
		trigger = {
			check_variable = { sunni_pop > atheism_pop }
			check_variable = { sunni_pop > bahai_pop }
			check_variable = { sunni_pop > buddhism }
			check_variable = { sunni_pop > catholic_pop }
			check_variable = { sunni_pop > caodaism_pop }
			check_variable = { sunni_pop > chinese_traditional_religion_pop }
			check_variable = { sunni_pop > coptic_pop }
			check_variable = { sunni_pop > druze_pop }
			check_variable = { sunni_pop > hinduism_pop }
			check_variable = { sunni_pop > ibadi_pop }
			check_variable = { sunni_pop > jainism_pop }
			check_variable = { sunni_pop > juche_pop }
			check_variable = { sunni_pop > judaism_pop }
			check_variable = { sunni_pop > native_religion_pop }
			check_variable = { sunni_pop > nestorian_pop }
			check_variable = { sunni_pop > orthodox_pop }
			check_variable = { sunni_pop > protestant_pop }
			check_variable = { sunni_pop > mormon_pop }
			check_variable = { sunni_pop > shia_pop }
			check_variable = { sunni_pop > shinto_pop }
			check_variable = { sunni_pop > sikhism_pop }
			#check_variable = { sunni_pop > sunni_pop }
			check_variable = { sunni_pop > tenrikyo_pop }
			check_variable = { sunni_pop > yazidi_pop }
			check_variable = { sunni_pop > zoroastrian_pop }
		}
		localization_key = sunni_majority_text
	}
	text = {
		trigger = {
			check_variable = { tenrikyo_pop > atheism_pop }
			check_variable = { tenrikyo_pop > bahai_pop }
			check_variable = { tenrikyo_pop > buddhism }
			check_variable = { tenrikyo_pop > catholic_pop }
			check_variable = { tenrikyo_pop > caodaism_pop }
			check_variable = { tenrikyo_pop > chinese_traditional_religion_pop }
			check_variable = { tenrikyo_pop > coptic_pop }
			check_variable = { tenrikyo_pop > druze_pop }
			check_variable = { tenrikyo_pop > hinduism_pop }
			check_variable = { tenrikyo_pop > ibadi_pop }
			check_variable = { tenrikyo_pop > jainism_pop }
			check_variable = { tenrikyo_pop > juche_pop }
			check_variable = { tenrikyo_pop > judaism_pop }
			check_variable = { tenrikyo_pop > native_religion_pop }
			check_variable = { tenrikyo_pop > nestorian_pop }
			check_variable = { tenrikyo_pop > orthodox_pop }
			check_variable = { tenrikyo_pop > protestant_pop }
			check_variable = { tenrikyo_pop > mormon_pop }
			check_variable = { tenrikyo_pop > shia_pop }
			check_variable = { tenrikyo_pop > shinto_pop }
			check_variable = { tenrikyo_pop > sikhism_pop }
			check_variable = { tenrikyo_pop > sunni_pop }
			#check_variable = { tenrikyo_pop > tenrikyo_pop }
			check_variable = { tenrikyo_pop > yazidi_pop }
			check_variable = { tenrikyo_pop > zoroastrian_pop }
		}
		localization_key = tenrikyo_majority_text
	}
	text = {
		trigger = {
			check_variable = { yazidi_pop > atheism_pop }
			check_variable = { yazidi_pop > bahai_pop }
			check_variable = { yazidi_pop > buddhism }
			check_variable = { yazidi_pop > catholic_pop }
			check_variable = { yazidi_pop > caodaism_pop }
			check_variable = { yazidi_pop > chinese_traditional_religion_pop }
			check_variable = { yazidi_pop > coptic_pop }
			check_variable = { yazidi_pop > druze_pop }
			check_variable = { yazidi_pop > hinduism_pop }
			check_variable = { yazidi_pop > ibadi_pop }
			check_variable = { yazidi_pop > jainism_pop }
			check_variable = { yazidi_pop > juche_pop }
			check_variable = { yazidi_pop > judaism_pop }
			check_variable = { yazidi_pop > native_religion_pop }
			check_variable = { yazidi_pop > nestorian_pop }
			check_variable = { yazidi_pop > orthodox_pop }
			check_variable = { yazidi_pop > protestant_pop }
			check_variable = { yazidi_pop > mormon_pop }
			check_variable = { yazidi_pop > shia_pop }
			check_variable = { yazidi_pop > shinto_pop }
			check_variable = { yazidi_pop > sikhism_pop }
			check_variable = { yazidi_pop > sunni_pop }
			check_variable = { yazidi_pop > tenrikyo_pop }
			#check_variable = { yazidi_pop > yazidi_pop }
			check_variable = { yazidi_pop > zoroastrian_pop }
		}
		localization_key = yazidi_majority_text
	}
	text = {
		trigger = {
			check_variable = { zoroastrian_pop > atheism_pop }
			check_variable = { zoroastrian_pop > bahai_pop }
			check_variable = { zoroastrian_pop > buddhism }
			check_variable = { zoroastrian_pop > catholic_pop }
			check_variable = { zoroastrian_pop > caodaism_pop }
			check_variable = { zoroastrian_pop > chinese_traditional_religion_pop }
			check_variable = { zoroastrian_pop > coptic_pop }
			check_variable = { zoroastrian_pop > druze_pop }
			check_variable = { zoroastrian_pop > hinduism_pop }
			check_variable = { zoroastrian_pop > ibadi_pop }
			check_variable = { zoroastrian_pop > jainism_pop }
			check_variable = { zoroastrian_pop > juche_pop }
			check_variable = { zoroastrian_pop > judaism_pop }
			check_variable = { zoroastrian_pop > native_religion_pop }
			check_variable = { zoroastrian_pop > nestorian_pop }
			check_variable = { zoroastrian_pop > orthodox_pop }
			check_variable = { zoroastrian_pop > protestant_pop }
			check_variable = { zoroastrian_pop > mormon_pop }
			check_variable = { zoroastrian_pop > shia_pop }
			check_variable = { zoroastrian_pop > shinto_pop }
			check_variable = { zoroastrian_pop > sikhism_pop }
			check_variable = { zoroastrian_pop > sunni_pop }
			check_variable = { zoroastrian_pop > tenrikyo_pop }
			check_variable = { zoroastrian_pop > yazidi_pop }
			#check_variable = { zoroastrian_pop > zoroastrian_pop }
		}
		localization_key = zoroastrian_majority_text
	}
}