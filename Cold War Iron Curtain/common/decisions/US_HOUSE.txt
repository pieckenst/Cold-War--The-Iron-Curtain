CONGRESS_US = {
	Communist_Control_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = communist_control_act
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.3 days = 25 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_ideas = Communist_Control_Act		
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = Communist_Control_Act
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }

	The_Mutual_Security_Act = {

		icon = generic_industry

	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = mutual_security_act
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.4 days = 25 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				every_country = {
						limit = {
							OR = {
								original_tag = COL
								original_tag = VEN
								original_tag = ECU
								original_tag = PRU
								original_tag = BRA
								original_tag = ARG
								original_tag = BOL
								original_tag = PAR
								original_tag = URG
								original_tag = CHL
							}
							NOT = { has_government = communism }
							NOT = { has_government = socialist }
							NOT = { has_government = National_Socialism }
							NOT = { has_government = maoism }
						}
								random_owned_controlled_state = {
										limit = {
												industrial_complex > 0
												free_building_slots = {
														building = industrial_complex
														size > 1
														include_locked = yes
													}
											}
										add_extra_state_shared_building_slots = 2
										add_building_construction = {
												type = industrial_complex
												level = 2
												instant_build = yes
											}
									}
								random_owned_controlled_state = {
										limit = {
												industrial_complex > 0
												free_building_slots = {
														building = industrial_complex
														size > 1
														include_locked = yes
													}
											}
										add_extra_state_shared_building_slots = 2
										add_building_construction = {
												type = industrial_complex
												level = 2
												instant_build = yes
											}
									}
					}		    
				} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
				every_country = {
						limit = {
							OR = {
								original_tag = COL
								original_tag = VEN
								original_tag = ECU
								original_tag = PRU
								original_tag = BRA
								original_tag = ARG
								original_tag = BOL
								original_tag = PAR
								original_tag = URG
								original_tag = CHL
							}
							NOT = { has_government = communism }
							NOT = { has_government = socialist }
							NOT = { has_government = National_Socialism }
							NOT = { has_government = maoism }
						}
								random_owned_controlled_state = {
										limit = {
												industrial_complex > 0
												free_building_slots = {
														building = industrial_complex
														size > 1
														include_locked = yes
													}
											}
										add_extra_state_shared_building_slots = 2
										add_building_construction = {
												type = industrial_complex
												level = 2
												instant_build = yes
											}
									}
								random_owned_controlled_state = {
										limit = {
												industrial_complex > 0
												free_building_slots = {
														building = industrial_complex
														size > 1
														include_locked = yes
													}
											}
										add_extra_state_shared_building_slots = 2
										add_building_construction = {
												type = industrial_complex
												level = 2
												instant_build = yes
											}
									}
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	Housing_Act_of_1949 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = housing_act_o_1949
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_ideas = housing_act_1949		
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = housing_act_1949
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	McCarran_Walter_Act_Override = {


		available = {
			has_country_flag = McCarran_Walter_Act
		}

		days_mission_timeout = 40

		activation = {
			has_country_flag = McCarran_Walter_Act
		}

		fixed_random_seed = no

		fire_only_once = yes

		is_good = no

		timeout_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 289  } 
						check_variable = { supseat_sen > 62 } 
					}
					news_event = { id = McCarran_Walter_Act.1 }
				    add_political_power = -30
					add_ideas = McCarran_Walter_Act
				}
				if = {
				    limit = { 
						check_variable = { supseat < 290 } 
						check_variable = { supseat_sen < 63 } 
					}
					add_political_power = 100
				    news_event = { id = McCarran_Walter_Act.2 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            				
		}
	}
	Central_Intelligence_Agency_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Central_Intelligence_Agency_Act_enabled
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 8 }
				country_event = { id = congress.3 days = 10 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_ideas = Central_Intelligence_Agency		
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = Central_Intelligence_Agency
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	Agricultural_Act_of_1949 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = agricultural_act_1949
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.7 days = 20 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_timed_idea = {
					idea = agricultural_price_controls
					days = 1200
				}
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_timed_idea = {
						idea = agricultural_price_controls
						days = 1200
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	Social_Security_Act_of_1950 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = social_security_act_of_1950
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.7 days = 20 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_ideas = social_security_act_1950		
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = social_security_act_1950
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	McCarran_Internal_Security_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = USA_McCarran_internal_security_act_enabled
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.3 days = 20 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_ideas = USA_McCarran_internal_security_act		
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = USA_McCarran_internal_security_act
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	Fair_Labor_Standards_Amendment = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Fair_Labor_Standards_Amendment_enabled
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.7 days = 20 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_timed_idea = {
					idea = increase_the_wages
					days = 1200
				}	
			} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_timed_idea = {
						idea = increase_the_wages
						days = 1200
					}						
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	Defense_Production_Act_of_1950 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Defense_Production_Act_of_1950_enabled
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.3 days = 20 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_offsite_building = { type = arms_factory level = 4 }
			} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_offsite_building = { type = arms_factory level = 4 }					
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_McGuire_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = McGuire_Act
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 1 }
				country_event = { id = congress.2 days = 5 }
				country_event = { id = congress.3 days = 10 }
			}

			
            effect_tooltip = {
			    add_political_power = 30
				add_offsite_building = { type = industrial_complex level = 2 }
			} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_offsite_building = { type = industrial_complex level = 2 }					
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_Omnibus_Medical_Research_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Omnibus_Medical_Research_Act
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 1 }
				country_event = { id = congress.2 days = 5 }
				country_event = { id = congress.3 days = 10 }
			}

			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas = medical_investments
			} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = medical_investments
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	The_National_Science_Foundation_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = National_Science_Foundation_Act
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 1 }
				country_event = { id = congress.2 days = 5 }
				country_event = { id = congress.3 days = 10 }
			}

			
            effect_tooltip = {
			    add_political_power = 30
				add_research_slot = 1
			} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_research_slot = 1
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	The_Anti_Merger_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Anti_Merger_Act
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 1 }
				country_event = { id = congress.2 days = 5 }
				country_event = { id = congress.7 days = 10 }
			}

			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas = usa_Anti_Merger_Act
			} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = usa_Anti_Merger_Act
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	The_Refugee_Relief_Act	= {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = refugee_relief_act_Vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 1 }
				country_event = { id = congress.3 days = 10 }
			}

			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas = refugee_relief_act
			} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = refugee_relief_act
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	The_Social_Security_Act_of_1953 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = social_security_act_1953_Vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 1 }
				country_event = { id = congress.2 days = 5 }
				country_event = { id = congress.7 days = 10 }
			}

			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas = social_security_act_1953
			} 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = social_security_act_1953
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	Federal_National_Mortgage_Association_Charter_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Federal_National_Mortgage_Association_Charter_Act_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        every_owned_state = {
					add_extra_state_shared_building_slots = 1
				}				
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_extra_state_shared_building_slots = 1
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }

	National_Housing_Act_of_1955 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = National_Housing_Act_of_1955
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        every_owned_state = {
					add_extra_state_shared_building_slots = 1
				}				
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_extra_state_shared_building_slots = 1
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	Agricultural_Act_of_1954 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = agricultural_act_of_1954
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 10 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
					add_tech_bonus = {
							name = agri_tech_bonus
							bonus = 0.10
							category = agri_tech
						}
					}							
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_tech_bonus = {
							name = agri_tech_bonus
							bonus = 0.10
							category = agri_tech
						}
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_Multiple_Mineral_Development_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = minderal_dev_act
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 10 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas =	Multiple_Mineral_Development_Act		
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas =	Multiple_Mineral_Development_Act	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_Small_Buisness_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Small_Buisness_Act_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 10 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas =	Small_Buisness_Act		
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas =	Small_Buisness_Act	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_Air_Pollution_Control_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Air_Pollution_Control_Act_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 10 }
				country_event = { id = congress.7 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas =	Air_Pollution_Control_Act		
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas =	Air_Pollution_Control_Act	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_Health_Research_Facilities_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Health_Research_Facilities_Act_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 10 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas =	Health_Research_Facilities_Act		
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas =	Health_Research_Facilities_Act	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_Interstate_Highway_Act_of_1955 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Interstate_Highway_Act_of_1955_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 10 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				every_owned_state = {
					add_building_construction = {
						type = infrastructure
						level = 2
						instant_build = yes
					}
				}				
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					every_owned_state = {
						add_building_construction = {
							type = infrastructure
							level = 2
							instant_build = yes
						}
					}	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	The_Fish_and_Wildlife_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Fish_and_Wildlife_Act_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 10 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 150
				add_stability = 0.05
			}				
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
					add_political_power = 150
					add_stability = 0.05
	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	The_National_Firearms_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = National_Firearms_Act_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas =	National_Firearms_Act		
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas =	National_Firearms_Act	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_Internal_Revenue_Code_of_1954 = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Internal_Revenue_Code_of_1954_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.2 days = 5 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas =	Internal_Revenue_Code_of_1954		
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas =	Internal_Revenue_Code_of_1954	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	The_Career_Incentive_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Career_Incentive_Act_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 10 }
				country_event = { id = congress.3 days = 15 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
				add_ideas =	Career_Incentive_Act		
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas =	Career_Incentive_Act	
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	Renew_Agricultural_Subsidies_and_Price_Controls = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Continue_Agricultural_Subsidies_Vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.7 days = 20 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_timed_idea = {
					idea = agricultural_price_controls
					days = 1200
				}
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_timed_idea = {
						idea = agricultural_price_controls
						days = 1200
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	Repeal_The_Taft_Hartley_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = repeal_taft_hartley
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        remove_ideas = taft_hartley_act
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					remove_ideas = taft_hartley_act
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	Fair_Labor_Stadnards_Amendment = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Fair_Labor_Stadnards_Amendment_Vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.7 days = 20 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_ideas = Fair_Labor_Standards
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_ideas = Fair_Labor_Standards
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	Rural_Healthcare_Act = {
	
        allowed = { tag = USA }
		
        available = {
            NOT = { has_country_flag = law_passing }
		   has_country_flag = Rural_Healthcare_Act_vote
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 0
        fire_only_once = yes
		
		complete_effect = {
	        set_country_flag = law_passing
			#custom_effect_tooltip = will_vote
			
			hidden_effect = {
				country_event = { id = congress.1 days = 5 }
				country_event = { id = congress.2 days = 15 }
				country_event = { id = congress.7 days = 20 }
			}
			
            effect_tooltip = {
			    add_political_power = 30
		        add_timed_idea = {
					idea = access_to_health_care
					days = 1200
				}
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > 216 } 
						check_variable = { supseat_sen > 47 } 
					}
					country_event = { id = us_congress.100 }
				    add_political_power = 30
					add_timed_idea = {
						idea = access_to_health_care
						days = 1200
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < 217 } 
						check_variable = { supseat_sen < 48 } 
					}
				    country_event = { id = parliament.1 }
				}
				set_variable = { supseat = 0 }
				set_variable = { supseat_sen = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
}






